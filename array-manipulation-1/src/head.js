/* exported head */
function head(array) {
  return array[0];
}

// after declaring the function "head" with expected parameter "array",
// return array at index 0 to get the first value in the array.
